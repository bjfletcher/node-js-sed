I've wanted something like "perl -e" for node js, for basic things like text replacements.  This is something that works, although it's not quite node -e yet...

node sed.js | data.txt

will process data.txt with the "sed" function inside sed.js.
